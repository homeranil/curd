export default function ({ $axios, app }) {
  // $axios.onRequest((config) => {
  //   config.headers.common['X-API-KEY'] =
  //     '21398ASD@!4312EASD@!$!CZCAScas90ua890cjaiofW#$%RTq23498u3'
  // })
  // $axios.onError((error) => {
  //   if (error.response.status === 401) {
  //     // app.$auth.logout()
  //   }
  // })
}
